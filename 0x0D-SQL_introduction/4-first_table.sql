-- Script that creates a table called first_table in the current database in the MySQL server
-- first_tabble description:
-- id INT, name VARCHAR(256)
CREATE TABLE IF NOT EXISTS first_table(
	id INT,
	name VARCHAR(256)
	);
