-- Script that lists all records of a table second_table of the database hbtn_0c_0 in the MySQL server
-- Should display both the score and the name 
-- Records should ordered by score(top first)
SELECT score, name FROM second_table ORDER BY score DESC;
