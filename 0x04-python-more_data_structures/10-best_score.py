#!/usr/bin/python3
def best_score(a_dictionary):
    if a_dictionary is None of a_dictionary == {}:
        return None
    score = max(a_dictionary, key=lambda g: a_dictionary[g])
            return score
